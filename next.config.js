/** @type {import('next').NextConfig} */
const nextConfig = {
  // Habilitar imágenes si se necesitan más adelante
  images: {
    unoptimized: true
  },
  // Configuración de compilación
  reactStrictMode: true,
};

module.exports = nextConfig;
